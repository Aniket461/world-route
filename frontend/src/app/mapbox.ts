/** Global Mapbox GL JS loaded from the CDN (avoids Angular/esbuild worker __async bugs). */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
declare const mapboxgl: any;

export default mapboxgl;
